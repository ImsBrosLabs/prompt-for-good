# Add runner readiness checks

The Runner Configuration Console exposes non-destructive Runner Readiness Checks for hub credentials, model provider credentials, GitHub credentials, structured configuration, and execution-limit validity. These checks give contributors a clear "ready to run" state before the runner starts or claims work, matching the fail-fast behavior when required configuration cannot be resolved.
