# Report redacted configuration snapshots

Contribution reports include a Redacted Configuration Snapshot in `contribution.details` so hub operators can diagnose provider, model, budget, verification, preference, and execution-limit differences across runs. Secret runtime configuration values are never reported to the hub; only non-secret values, presence metadata, and a snapshot version or hash are included.
