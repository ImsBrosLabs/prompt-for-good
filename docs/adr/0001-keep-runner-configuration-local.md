# Keep runner configuration local

The agent configuration admin is a local Runner Configuration Console, not a central hub administration surface. Runner-owned policy controls how a contributor spends local machine time, GitHub access, and LLM quota, so the console manages local runner configuration while the hub receives only the dispatch preferences and status that belong in the Runner Work Protocol.
