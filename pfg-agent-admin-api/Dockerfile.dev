FROM node:22-bookworm-slim

WORKDIR /workspace

COPY pfg-runtime-config-core /pfg-runtime-config-core
COPY pfg-agent-admin-api/package*.json ./
RUN npm install

COPY pfg-agent-admin-api .

EXPOSE 8091

CMD ["sh", "-c", "npm run db:migrate && npm run dev -- --host 0.0.0.0"]
